export declare class MyComponent {
  component: HTMLElement;
  url_logo: string;
  show_mask: string;
  background_color: string;
  disable_control_panel: string;
  stop_after_capturing: string;
  face_detection: string;
  model_path: string;
  probability_threshold: string;
  debug: string;
  logo_style: string;
  private _resolutionOnPhoto;
  private _cameraStatus;
  private _mobileMakePhoto;
  private _stopAfterCapturing;
  private _showMask;
  private _disable_control_panel;
  private _face_detection;
  private _probabilityThreshold;
  private _debug;
  private _device;
  private _mobile;
  videoStarted(): void;
  retakePhoto(): Promise<void>;
  takePhoto(): void;
  openCamera(): Promise<void>;
  loadModels(event: CustomEvent<any>): Promise<void>;
  errorCamera(error: any): void;
  closeCamera(): void;
  makePhoto(): void;
  updateState: () => void;
  constructor();
  componentWillLoad(): void;
  componentDidUpdate(): void;
  photoIsReady: (photos: any) => void;
  enableButton: () => void;
  video: HTMLVideoElement;
  canvas: HTMLCanvasElement;
  render(): any;
}
